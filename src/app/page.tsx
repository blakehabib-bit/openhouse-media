// The homepage now renders the V3 design (previously previewed at /v2).
// The component lives in src/app/v2/page.tsx; this re-exports it as "/".
// NOTE: this design's copy is in code (not Tina-editable yet) — wiring it
// into the CMS is a follow-up.
export { default } from "./v2/page";
